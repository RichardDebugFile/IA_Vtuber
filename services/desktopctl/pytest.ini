[pytest]
testpaths = tests
markers =
    unit: Tests unitarios — sin servicios activos, mocks totales
    integration: Tests de integración — flujos completos entre módulos internos
asyncio_mode = auto
